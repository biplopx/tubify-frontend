import React from 'react';

const ListRecommendations = ({recommendation}) => {
    console.log(recommendation);
    
    return (
        <div>

        </div>
    );
};

export default ListRecommendations;